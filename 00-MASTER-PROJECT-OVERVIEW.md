# 00 — Master Project Overview: ClientDock

> **Last Audited:** Post-Migration PR (September 2026)
> **Auditor:** Antigravity — Senior Technical Review
> **Product Name:** ClientDock
> **Repository:** `d:\Programming\ClientDock\ClientDock\SAAS`

---

## What Is ClientDock?

ClientDock is a **multi-tenant SaaS client portal** built for agencies, freelancers, and high-ticket service providers. It solves the painful problem of client onboarding: collecting files, credentials, approvals, and assets without the chaos of email chains, Slack threads, and Google Drive links.

### Core Value Proposition

> *"Stop chasing clients for files. Give them a simple, secure portal. Get everything you need, organized."*

### Target Market

| Segment | Description |
|---|---|
| **Primary** | Digital agencies (web, marketing, SEO, creative) |
| **Secondary** | Freelancers (developers, designers, copywriters) |
| **Tertiary** | Consultants and high-ticket service providers |
| **Geographic Focus** | US market (primary), India (secondary) |

---

## Tech Stack (Post-Migration — Evidence-Based)

| Layer | Technology | Version |
|---|---|---|
| Framework | Next.js App Router | 16.2.9 |
| Language | TypeScript | ^5 |
| Database | **PostgreSQL via `pg`** | ^8.11.3 |
| ORM | Drizzle ORM | ^0.45.2 |
| Auth | Better Auth | ^1.6.20 |
| File Upload | **Cloudflare R2 (AWS S3 SDK)** | ^3.540.0 |
| Email | **Brevo (`@getbrevo/brevo`)** | ^2.1.1 |
| Billing | **Dodo Payments (REST API)** | — (no npm package) |
| Caching/Rate Limit | Redis | ^6.0.0 |
| Styling | Tailwind CSS | v4 |
| Testing | Vitest | ^4.1.9 |
| Drag and Drop | @hello-pangea/dnd | ^18.0.1 |
| Icons | Lucide React | ^1.21.0 |
| Containerization | Docker + Docker Compose | — |
| CI/CD | GitHub Actions | — |

> **Migration Note:** SQLite/Litestream, Razorpay, UploadThing, and Resend have been **fully removed** as of the September 2026 migration. See `SAAS/MIGRATION_FINAL.md` for the full log.

---

## Application Architecture

```
User (Better Auth identity)
 └─ Agency (one per user, auto-created on signup)
     ├─ Client (invited by agency, unique portal token)
     │   ├─ Project (one per engagement)
     │   │   ├─ ChecklistItem (requirements with approval workflow)
     │   │   │   └─ Submission (client-uploaded files via Cloudflare R2)
     │   │   ├─ ActivityLog (audit trail)
     │   │   └─ TimelineEvents (milestones)
     │   └─ ... (more projects)
     ├─ Lead (CRM pipeline entry)
     │   └─ LeadActivityLog
     ├─ Meeting (scheduled meetings)
     ├─ Notification (in-app notifications)
     └─ Subscription / Payment / WebhookEvent (Dodo Payments billing)
```

### Multi-tenancy Model
Every meaningful database record is scoped to an `agency_id`. The application enforces this via:
1. `getCurrentSession()` → gets authenticated user + agency
2. `authorizeProjectAccess()` / `authorizeClientAccess()` → throws on cross-tenant access
3. All queries use `WHERE agency_id = ?` or join through client → agency

---

## Repository Structure

```
SAAS/
├── src/
│   ├── app/
│   │   ├── actions/         # Server actions (auth, projects, leads, email, notifications, settings)
│   │   ├── api/             # API routes (auth, billing/Dodo, cron/reminders, clients, projects)
│   │   ├── dashboard/       # Agency dashboard (billing, analytics, leads, projects, clients, files, meetings, notifications, settings, account)
│   │   ├── portal/[token]/  # Client portal (passwordless token-based)
│   │   ├── blog/            # Marketing blog (7 articles)
│   │   ├── components/      # Landing page sections (Hero, Pricing, FAQ, Testimonials, Footer, etc.)
│   │   ├── legal/           # Privacy, Terms, GDPR, Cookies
│   │   ├── login/           # Login page
│   │   ├── signup/          # Signup page
│   │   └── [other pages]    # about, careers, changelog, community, contact, docs, help, r/
│   ├── components/          # Shared UI (auth forms, billing badges, sidebar)
│   ├── db/                  # PostgreSQL schema + Drizzle index
│   ├── lib/                 # Utilities (auth, db helpers, data, dodo, rate-limit, redis, r2, email)
│   ├── styles/              # Global CSS
│   └── test/                # Test files and mock data
├── drizzle-pg/              # PostgreSQL SQL migrations (generated by drizzle-kit)
├── docker-compose.yml       # Docker Compose: Postgres + Redis + Next.js app
├── Dockerfile               # Multi-stage production Docker build
├── .env.example             # All required environment variables documented
└── .github/workflows/       # CI pipeline
```

---

## Current Completion Assessment

> **Post-Migration Status: ~80% Production Ready**
>
> The stack migration is complete. Multiple P0 bugs were found and fixed in the September 2026 code review.

| Area | Completion | Notes |
|---|---|---|
| Auth (Login/Signup/OAuth) | 95% | P0 missing-await bug fixed; rate limiting on Redis |
| Agency Dashboard | 85% | Real DB queries; meetings widget shows demo data |
| Client Portal | 80% | R2 file upload wiring needs end-to-end test |
| Checklist/Approval Workflow | 95% | Core flow working; rejection email has no portal link |
| File Upload (R2) | 70% | Presign API + helpers exist; UI integration needs verification |
| Notifications | 60% | `file_uploaded` type not triggered; deadline check runs in cron |
| Leads CRM | 90% | Full CRUD; ownership verification enforced |
| Analytics | 45% | Point-in-time only; no time-series; no revenue data |
| Meetings | 30% | Schema + UI exist; no server actions (create/update/delete) |
| Billing (Dodo Payments) | 85% | Webhook + checkout working; premature upgrade bug fixed |
| Settings | 80% | Notification preferences work; profile edit incomplete |
| Landing Page | 90% | Full sections functional |
| Blog | 90% | 7 articles; static content |
| Legal Pages | 90% | Content exists |
| Testing | 50% | Tests reference old stack; need review |
| Docker/Deployment | 85% | Postgres + Redis Compose; no Litestream (removed with SQLite) |
| Documentation | 75% | Updated in this review; some sub-docs still reference old providers |

---

## What Already Works Well

1. **Multi-tenant isolation** — Agency scoping enforced at server action and query layers
2. **Auth system** — Better Auth with email/password + optional Google/GitHub OAuth
3. **Client portal** — Token-based passwordless access; SHA-256 hash + 30-day expiry
4. **Checklist/approval workflow** — Add item → client uploads → agency approves/rejects with reason + email
5. **Dodo Payments** — Checkout, HMAC webhook verification, idempotency, plan gating all correct
6. **Database schema** — Clean PostgreSQL schema with cascades, indexes, and full tenant isolation
7. **Plan enforcement** — `check-plan.ts` and `plan-config.ts` — single source of truth, used consistently
8. **Error handling** — `withDbErrorHandling`, `ActionResult<T>`, Zod validation — consistent pattern
9. **Brevo email** — Gracefully logs in dev when key is absent; no crashes
10. **Leads CRM** — Full Kanban pipeline, stage history, convert-to-client

## Known Issues (Post-Migration)

| Bug | Severity | Status |
|---|---|---|
| Missing `await` on `createAgency` | P0 | ✅ Fixed |
| `BYPASS_DASHBOARD_AUTH=True` case mismatch | P0 | ✅ Fixed |
| `verifyWebhook` crash on malformed signature | P0 | ✅ Fixed |
| Dodo API errors swallow response body | P0 | ✅ Fixed |
| Redis polling loop no timeout | P0 | ✅ Fixed |
| `setCache` non-atomic TTL | P1 | ✅ Fixed |
| R2 filename path traversal | P1 | ✅ Fixed |
| Premature plan upgrade on `subscription.created` | P1 | ✅ Fixed |
| `CRON_SECRET` missing from env files | P1 | ✅ Fixed |
| Dev bypass incompatible with authorization checks | P1 | ⚠️ Open |
| `ActionResult<T>` duplicated in 4 files | P2 | ⚠️ Open |
| Meetings CRUD server actions missing | P2 | ⚠️ Open |
| LIKE-based reminder detection fragile | P2 | ⚠️ Open |
| `BETTER_AUTH_URL` hardcoded to localhost in Compose | P2 | ⚠️ Open |

---

## Priority Action List (High Level)

```
P1 — Verify R2 file upload UI end-to-end (presign → browser upload → URL saved)
P1 — Fix dev bypass to work with authorization (seed test data or bypass auth guards)
P2 — Centralize ActionResult<T> type to src/lib/action-result.ts
P2 — Add meetings server actions (create, update, delete)
P2 — Add time-series data for analytics
P3 — Complete settings page (profile/agency name editing)
P4 — White-labeling features
P4 — Team members feature
```

---

## Document Index

| Document | Path |
|---|---|
| Current State | [01-CURRENT-STATE.md](./01-CURRENT-STATE.md) |
| Codebase Understanding | [02-CODEBASE-UNDERSTANDING.md](./02-CODEBASE-UNDERSTANDING.md) |
| Feature Inventory | [03-FEATURE-INVENTORY.md](./03-FEATURE-INVENTORY.md) |
| Completion Assessment | [04-COMPLETION-ASSESSMENT.md](./04-COMPLETION-ASSESSMENT.md) |
| Database Overview | [database/DATABASE-OVERVIEW.md](./database/DATABASE-OVERVIEW.md) |
| Database Schema | [database/DATABASE-SCHEMA.md](./database/DATABASE-SCHEMA.md) |
| Database Security | [database/DATABASE-SECURITY.md](./database/DATABASE-SECURITY.md) |
| Bugs | [bugs/BUGS.md](./bugs/BUGS.md) |
| UX Audit | [ux/UX-AUDIT.md](./ux/UX-AUDIT.md) |
| Security Audit | [security/SECURITY-AUDIT.md](./security/SECURITY-AUDIT.md) |
| Architecture Audit | [architecture/ARCHITECTURE-AUDIT.md](./architecture/ARCHITECTURE-AUDIT.md) |
| Product Understanding | [product/PRODUCT-UNDERSTANDING.md](./product/PRODUCT-UNDERSTANDING.md) |
| Monetization | [product/MONETIZATION.md](./product/MONETIZATION.md) |
| US Market | [research/US-MARKET.md](./research/US-MARKET.md) |
| Competitor Analysis | [research/COMPETITOR-ANALYSIS.md](./research/COMPETITOR-ANALYSIS.md) |
| Infrastructure | [infrastructure/CURRENT-INFRASTRUCTURE.md](./infrastructure/CURRENT-INFRASTRUCTURE.md) |
| Master Roadmap | [ROADMAP.md](./ROADMAP.md) |


